# Reel App Backend

Minimal Express + TypeScript backend scaffold. To run locally:

```bash
cd backend
npm install
npm run dev
```

Endpoints:
- GET / -> health
- GET/POST /users
- GET/POST/PUT/DELETE /tasks
